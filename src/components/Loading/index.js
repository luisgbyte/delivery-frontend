import React from 'react';

import { Container, Spiner } from './styles';

function Loading() {
    return (
        <Container>
            <Spiner />
        </Container>
    );
}

export default Loading;
